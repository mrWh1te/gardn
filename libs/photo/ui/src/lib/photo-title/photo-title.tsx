import styled from '@emotion/styled';

export const PhotoTitle = styled.h6`
  width: 100%;
  padding: 1rem 2rem;
  box-sizing: border-box;
  font-weight: 300;
  text-align: right;
`;

export default PhotoTitle;