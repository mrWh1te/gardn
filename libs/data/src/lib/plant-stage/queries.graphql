query getPlantStage($id: Int!) {
  plantStage(id: $id) {
    name,
    dateCreated,
    description,
    environment {
      id, name
    }
  }
}

query getAllPlantStages {
  plantStages {
    name,
    dateCreated,
    description,
    environment {
      id, name
    }
  }
}