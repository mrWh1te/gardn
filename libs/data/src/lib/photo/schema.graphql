type Photo {
  id: Int!
  url: String!
  title: String
  dateCreated: Timestamp 
}

type Query {
  photos: [Photo]
  photo(id: Int!): Photo
}

type Mutation {
  addPhoto(url: String, title: String): Photo
}