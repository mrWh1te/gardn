query getLifeCycle($id: Int!) {
  lifeCycle(id: $id) {
    name,
    dateCreated,
    description,
    environment
  }
}

query getAllLifeCycles {
  lifeCycles {
    name,
    dateCreated,
    description,
    environment
  }
}