type LifeCycle implements BaseDbModel {
  id: Int!
  dateCreated: Timestamp!
  
  name: String!
  description: String
  environment: Environment
}

type Query {
  lifeCycles: [LifeCycle]
  lifeCycle(id: Int!): LifeCycle
}