query getPlants {
  plants {
    id, name
  }
}

query getPlant($id: Int!) {
  plant(id: $id) {
    name
  }
}

query getSpecies($id: Int!) {
  species(id: $id) {
    name, description
  }
}

query getAllSpecies {
  allSpecies {
    id, name
  }
}