query getPlants {
  plants {
    id, name
  }
}

query getPlant($id: Int!) {
  plant(id: $id) {
    name,
    species {
      name
    },
    dateCreated,
    dateGerminated,
    datePlanted,
    dateSprouted,
    photos {
      url, title
    },
    avatar {
      url
    }
  }
}
