query getPlants {
  plants {
    id, name
  }
}

query getPlant($id: Int!) {
  plant(id: $id) {
    name,
    species {
      name
    },
    dateCreated,
    dateGerminated,
    datePlanted,
    dateSprouted,
    photos {
      url, title
    },
    coverPhoto {
      url
    }
  }
}

query getPlantPhotos($id: Int!) {
  plant(id: $id) {
    photos {
      id, url, title, dateCreated
    }
  }
}
