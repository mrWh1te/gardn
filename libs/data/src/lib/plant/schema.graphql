type Plant {
  id: Int!
  name: String
  species: Species
  dateCreated: Timestamp
  photos: [Photo]
  coverPhoto: Photo
  avatar: Photo

  currentLifeCycle: LifeCycle
}

type Query {
  plants: [Plant]
  plant(id: Int!): Plant
}

type Mutation {
  addPlant(name: String): Plant
}
