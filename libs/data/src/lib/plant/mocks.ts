import { createPlant } from './helpers/create-plant';

export const mockPlant1 = createPlant({name: 'Jane'});
export const mockPlant2 = createPlant({name: 'John'});
export const mockPlant3 = createPlant({name: 'Dracula'})